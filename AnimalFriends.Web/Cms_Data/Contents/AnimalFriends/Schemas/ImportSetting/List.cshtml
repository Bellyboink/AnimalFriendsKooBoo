<h3>ImportSetting</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["ConnectionString"], "ImportSetting/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>