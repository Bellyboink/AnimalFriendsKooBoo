<h3>StartPageBanner</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["Image"], "StartPageBanner/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>