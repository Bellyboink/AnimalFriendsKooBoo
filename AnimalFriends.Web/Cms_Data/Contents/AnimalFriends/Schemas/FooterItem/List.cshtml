<h3>FooterItem</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["Header"], "FooterItem/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>